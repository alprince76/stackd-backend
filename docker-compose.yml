services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: stackd
      POSTGRES_PASSWORD: stackd
      POSTGRES_DB: stackd
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
